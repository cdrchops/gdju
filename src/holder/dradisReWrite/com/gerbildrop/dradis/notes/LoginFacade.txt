package com.gerbildrop.dradis.messaging;

import java.util.Map;
import java.util.HashMap;

/**
 * @author timo
 * @version ${1.1}
 * @since Oct 14, 2006 3:33:43 PM
 */
public class LoginFacade {
    private static final Map umap = new HashMap();

    static {
        umap.put("hoff", "05137MO");
    }

    public static boolean checkLogin(String uname, String pass) {
        if (umap.containsKey(uname)
            && umap.containsValue(pass)) {
            String pass2 = (String) umap.get(uname);
            System.out.println("every thing is good baby " + pass2.equals(pass));
            return pass2.equals(pass);
        } else {

        }
        return true;
    }
}
